[{"assignees":[],"body":"## Bug\n\nIn `pkg/mapper.mapTestJSONSection`, the call `results, _, err := testjson.ParseBytes(sec.Content)` discards the malformed line count (second return value).\n\nThe top-level go test path warns when malformed lines are skipped (`cmd/fo.parseInput`), but the report section path does not.\n\n## Reachability\n\n`cmd/fo.run` → `parseInput(detect.Report)` → `mapper.FromReport` → `mapTestJSONSection`\n\n## Failure scenario\n\nAggregated report has truncated test-json lines due to transport/log splitting. Section parser skips malformed failures and emits a mostly-passing summary — masking real failures.\n\n## Fix\n\nSurface malformed count as a `pattern.Error` or warning metric in the report section summary.\n\n## Verification\n\nGolden report fixture with malformed test-json lines; assert warning/error presence in output.\n\n---\nSource: Codex 3-pass bug audit (PR #231)","closedAt":null,"createdAt":"2026-03-29T15:57:18Z","labels":[{"id":"LA_kwDOOpIJEM8AAAACAOfdBQ","name":"bug","description":"Something isn't working","color":"d73a4a"}],"number":239,"state":"OPEN","title":"Report mode silently drops malformed testjson lines (false-clean risk)","updatedAt":"2026-03-29T15:57:18Z"},{"assignees":[],"body":"## Bug\n\n`internal/report.Parse` splits only on `\\n`; each line from CRLF input retains trailing `\\r`. The delimiter regex is anchored (`^...$`) and does not tolerate `\\r`, so delimiter lines fail to match.\n\n## Reachability\n\n`cmd/fo.run` → `detect.Sniff` identifies report → `report.Parse` fails delimiter match on `\\r`-terminated lines.\n\n## Failure scenario\n\nA report generated on Windows (`\\r\\n`) is piped into `fo`; parser returns `ErrNoSections`, exiting with code 2 despite valid content.\n\n## Fix\n\nNormalize line endings (`\\r\\n` → `\\n`) before parsing, or trim terminal `\\r` per line before regex match.\n\n## Verification\n\nAdd parser and end-to-end CLI fixture test for CRLF report input.\n\n---\nSource: Codex 3-pass bug audit (PR #231)","closedAt":null,"createdAt":"2026-03-29T15:56:51Z","labels":[{"id":"LA_kwDOOpIJEM8AAAACAOfdBQ","name":"bug","description":"Something isn't working","color":"d73a4a"}],"number":238,"state":"OPEN","title":"CRLF report delimiters fail to parse (Windows cross-platform bug)","updatedAt":"2026-03-29T15:56:51Z"},{"assignees":[],"body":"## Bug\n\nIn batch mode, `pkg/testjson.aggregator.processEvent` appends every `output` line into `pkg.outputBuf[e.Test]`. On `pass`/`skip`, counters are incremented but the test's output buffer is never cleared.\n\nUnlike stream mode (`pkg/stream`), the batch aggregator does not reclaim output for passing/skipped tests.\n\n## Reachability\n\n`cmd/fo.run` → `parseInput(detect.GoTestJSON)` → `testjson.ParseBytes` → `ParseStream` → `aggregator.processEvent`\n\n## Failure scenario\n\nCI runs `go test -json ./...` with verbose subtests across many packages. Process RSS grows with every passing test log line and can OOM or trigger container eviction.\n\n## Fix\n\nIn `processEvent`, delete `pkg.outputBuf[e.Test]` on terminal test actions (`pass`, `skip`, and after copying on `fail`).\n\n## Verification\n\nStress test generating many passing tests with output; assert bounded map size after package completion.\n\n---\nSource: Codex 3-pass bug audit (PR #231)","closedAt":null,"createdAt":"2026-03-29T13:22:44Z","labels":[{"id":"LA_kwDOOpIJEM8AAAACAOfdBQ","name":"bug","description":"Something isn't working","color":"d73a4a"},{"id":"LA_kwDOOpIJEM8AAAACdPxlMg","name":"in-progress","description":"","color":"c5def5"}],"number":237,"state":"OPEN","title":"Unbounded memory in batch testjson: passing test output buffers never freed","updatedAt":"2026-03-29T16:17:44Z"}]
